{
  "name": "commerce-packages",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "playground",
    "storybook"
  ],
  "scripts": {
    "build": "bun run --filter '@storefront/*' build",
    "typecheck": "bun run --filter '@storefront/*' typecheck",
    "smoke": "bun run --cwd playground smoke",
    "storybook": "bun run --cwd storybook dev"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
